# Hermes Kit — Personas

> All personas are defined inline in `config.yaml` under `agent.personalities`.
> This directory exists as a placeholder for future persona files
> that may be loaded dynamically (e.g. YAML/JSON per persona).
>
> For the current persona prompt map used by Autarch, see:
> `src/services/personaPrompts.ts` (Single Source of Truth)
